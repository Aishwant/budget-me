from django.conf.urls import url
from budgetmeapi import views

urlpatterns = [
    # url(r'^api/user$', views.users_list),
    # url(r'^api/user/(?P<pk>[0-9]+)$', views.user_detail),
    # url(r'^api/user/')
]